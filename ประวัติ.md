# ประวัติเวอร์ชัน — เกมท้ายตัวเลข (Tualek)

ไฟล์นี้เป็นบันทึกการแก้และรูปแบบระบบทั้งหมด  
ใช้สำหรับ **ย้อนงานเมื่อแก้ผิด** และให้ **AI / คนพัฒนาคนอื่น** ทำงานต่อเนื่องได้

**เวอร์ชันปัจจุบัน: V1.2.02**  
แสดงท้ายหน้าเว็บเล็กๆ เป็น `V1.2.02` (จาก `version.js` → `window.TUALEK_VERSION`)

Repo: https://github.com/studio22mailme/number-guess-game  
Deploy: https://tualek.onrender.com

---

## กฎสำหรับรอบแก้ถัดไป (สำคัญมาก)

ทุกครั้งที่แก้โค้ด ต้องทำครบ 4 ข้อ:

1. เพิ่มเลขเวอร์ชันตามรูปแบบ **V1.x.xx** (เช่น `1.2.00` → `1.2.01` แก้เล็ก / `1.3.00` ฟีเจอร์ใหญ่)
2. อัปเดต `version.js` (`window.TUALEK_VERSION`)
3. อัปเดต `"version"` ใน `package.json` ให้สอดคล้อง (semver เช่น `1.2.0`)
4. **เพิ่มหัวข้อเวอร์ชันใหม่ด้านบนสุดของส่วน “บันทึกรายรอบ”** ในไฟล์นี้ พร้อม:
   - วันที่
   - git commit hash (หลัง push)
   - สรุปสิ่งที่เปลี่ยน
   - ไฟล์ที่แตะ
   - วิธีย้อน (checkout hash เดิม)

อย่า commit ไฟล์ลับ: `.env`, service account, คีย์ Firebase

---

## โครงสร้างโปรเจกต์ (ภาพรวม)

| ไฟล์ | หน้าที่ |
|------|---------|
| `index.html` | โครงหน้าจอทั้งหมด (login / setup / lobby / gate / play / result) |
| `styles.css` | สไตล์ + ธีมอายุ (`html[data-age-theme=...]`) |
| `game.js` | ลอจิกเกมฝั่ง client, WebSocket, หน้าจอ, แชร์, ยืนยันออก |
| `server.js` | HTTP + WebSocket: ห้องออนไลน์, บอท, รอบ, ชนะ |
| `auth.js` | Firebase / โหมดทดลอง (guest) |
| `age.js` | อายุ → ปีเกิด → ธีม |
| `music.js` | เพลงพื้นหลัง |
| `firebase-config.js` | ค่า config ฝั่งเว็บ |
| `version.js` | เลขเวอร์ชันที่โชว์บนเว็บ |
| `ประวัติ.md` | ไฟล์นี้ |
| `README.md` | วิธีรัน / ตั้งค่า Firebase / Deploy |

**สแต็ก:** Node.js + `ws` + static front · Deploy Render · Auth แบบ guest ได้ (`AUTH_REQUIRED` ฝั่งเซิร์ฟเวอร์ปิดบังคับได้)

---

## กฎเกม / พฤติกรรมสำคัญ (สถานะปัจจุบัน)

### โหมด
- **คนเดียว** — เล่นคนเดียวบนเครื่อง
- **ส่งเครื่อง** — สลับผู้เล่นบนเครื่องเดียวกัน (มีหน้า gate / นับถอยหลัง ส่งต่อได้ด้วยปุ่มถัดไป)
- **คนละเครื่อง** — ห้อง WebSocket, ล็อบบี้, พร้อม/เตะ, แชร์ลิงก์

### ความยาก
- ง่าย / ปกติ / ยาก / ยากมาก — จำนวนหลัก, ซ้ำได้ไหมตามตั้งค่า, feedback แบบคอลัมน์ในโหมดง่าย, เวลาต่อบรรทัด

### ออนไลน์ (สำคัญ)
- **ไม่มีบอทแทนคน** เมื่อหมดเวลา → แถวว่าง (skipped)
- **บอทแข่งในล็อบบี้** — หัวห้องเพิ่ม/เตะได้ สูงสุดรวมผู้เล่น 8
- บอททายอัตโนมัติแต่ละรอบ; แสดง **กระดาษบอทย่อ** ใต้ปุ่มออกจากเกม
- หลุดจากล็อบบี้มี grace ~45 วิ; ตอนเล่น ~90 วิ; หัวห้องโอนต่ออัตโนมัติ; เหลือน้อยกว่า 2 คน → ห้องเลิก
- Guest UID คงที่ใน `localStorage` (`tualek-demo-uid`)
- จำตั้งค่า/ที่นั่งห้อง: `tualek-setup-session`, `tualek-room-session`
- แชร์ล็อบบี้: ปุ่ม **คัดลอกลิงก์** + **แชร์** (ไอคอน) เท่านั้น
- ยืนยันก่อนออก: กลับเบราว์เซอร์ / ปิดแท็บ / ออกจากห้อง / ออกจากเกม

### อื่นๆ
- ธีมอายุจากปีเกิด (อายุขยับตามปีปฏิทิน)
- เพลงพื้นหลัง: เปิดได้หลัง gesture แรก
- ลีดเดอร์บอร์ดแยกตามโหมด + ระดับความยาก; แสดงจำนวนชนะและนาทีเฉลี่ยต่อเกม
- หัวข้อสถิติ: `อันดับชนะ - ระดับง่าย|ปกติ|ยาก|ยากมาก`
- ปุ่มโหมดแสดงจำนวนคนที่กำลังเล่นในโหมดนั้น
- Tagline หน้าตั้งค่า: `ฝึกสมองเล่นสนุกคนเดียวและแข่งกับเพื่อน`

---

## วิธีย้อนเวอร์ชันด้วย git

```powershell
cd "C:\Users\Admin\Documents\เกมท้ายตัวเลข"
git log --oneline
git checkout <commit-hash>
```

หรือสร้าง branch ย้อนเฉพาะรอบ:

```powershell
git checkout -b restore-V1.0.51 <commit-hash>
```

กลับมาล่าสุดบน main:

```powershell
git checkout main
git pull
```

---

## บันทึกรายรอบ

### V1.2.02 — 2026-09-13
**ธีม:** แก้ปุ่มตัวเลขสุดท้ายไม่เทาในโหมดไม่ซ้ำ  
**Commit:** *(ใส่ว่าหลัง push)*  
**ไฟล์:** `game.js`, `version.js`, `index.html`, `package.json`, `ประวัติ.md`

- `updateKeypadState` นับเลขใน draft ทุกช่องรวมช่องที่ caret อยู่

---

### V1.2.01 — 2026-09-13
**ธีม:** เวอร์ชันแปะกับเนื้อหาเว็บ ไม่ลอยทับจอ  
**Commit:** `1d10ddd`  
**ไฟล์:** `index.html`, `styles.css`, `version.js`, `package.json`, `game.js`, `ประวัติ.md`

- ย้าย `V…` เข้าใน `.desk` ท้ายหน้าจอ
- เลิกใช้ `position: fixed`

---

### V1.2.00 — 2026-09-13
**ธีม:** ปรับตั้งค่าหน้าแรก · สถิติตามระดับ · ธีมอายุลึกขึ้น · นับคนต่อโหมด  
**Commit:** `239cd26`  

**ไฟล์:** `index.html`, `styles.css`, `game.js`, `server.js`, `auth.js`, `age.js`, `version.js`, `package.json`, `ประวัติ.md`

- ปุ่มโหมดแสดงจำนวนคนเล่นปัจจุบัน เช่น `คนเดียว (5)`
- ปุ่ม Reset อายุใต้แบดจ์ออนไลน์ → เปิดกรอกอายุใหม่
- ธีมอายุปรับปุ่ม / กระดาษ / ตาราง ไม่ใช่แค่สี
- ข้อความตั้งค่า: ตัวเลขซ้ำ, จำนวนรอบคำตอบ, จำกัด N รอบ, hint เวลาออนไลน์
- ไม่ใส่ชื่อผู้เล่นอัตโนมัติ; placeholder ส่งเครื่อง = พิมพ์ชื่อผู้เล่น
- ชื่อในห้องว่างก่อนล็อบบี้; ชื่อห้องว่าง = วันเวลา `DDMMYYYYHHmm`
- รหัสผ่านไม่ซ้อนในแถวรหัสห้อง; กรอบล็อบบี้ออนไลน์ชัดขึ้น
- สถิติแยกตามระดับความยาก + แสดงนาทีเฉลี่ยต่อเกม; หัวข้อ `อันดับชนะ - ระดับ…`
- ข้อความรอบโหมดคนละเครื่องให้เหมือนโหมดอื่น

---

### V1.1.00 — 2026-09-13
**ธีม:** ระบบเวอร์ชัน + ไฟล์ประวัติ + แสดงเลขเวอร์ชันท้ายเว็บ  
**Commit:** *(ใส่ว่าหลัง push)*  
**ไฟล์:** `ประวัติ.md`, `version.js`, `index.html`, `styles.css`, `game.js`, `package.json`

- สร้างไฟล์ประวัติสำหรับย้อนงานและให้ AI ศึกษาต่อ
- เพิ่ม `version.js` เป็นแหล่งเลขเวอร์ชันเดียว
- แสดง `V1.1.00` เล็กๆ ท้ายหน้าเว็บ
- กำหนดรูปแบบเวอร์ชัน **V1.x.xx** สำหรับทุกครั้งที่แก้

---

### V1.0.52 — 2026-09-13 · `5d183ca`
**ธีม:** ปรับข้อความ tagline  
**ไฟล์:** `index.html`, `game.js`

- เปลี่ยนเป็น `ฝึกสมองเล่นสนุกคนเดียวและแข่งกับเพื่อน`

---

### V1.0.51 — 2026-09-12 · `849a511`
**ธีม:** แชร์ / กระดาษบอท / ยืนยันออก  
**ไฟล์:** `index.html`, `styles.css`, `game.js`, `server.js`

- ล็อบบี้เหลือปุ่ม **คัดลอกลิงก์** + **แชร์** (มีไอคอน) เอาไลน์/เฟซบุ๊กออก
- ส่ง `botPapers` จากเซิร์ฟเวอร์; แสดงกระดาษบอทย่อใต้ปุ่มออกจากเกม
- หน้าต่างยืนยันก่อนออกจากห้อง/เกม/กดกลับ/ปิดแท็บ
- Tagline รอบก่อน: `ฝึกสมองสนุกได้ทั้งเล่นคนเดียวและแข่งกับเพื่อน`

---

### V1.0.50 — 2026-09 · `e98a81e`
**ธีม:** จำที่นั่งห้อง / guest UID คงที่  
**ไฟล์:** `game.js`, `auth.js` (และที่เกี่ยวข้อง)

- Persist setup + room session ข้าม refresh
- Guest identity คงที่ (`tualek-demo-uid`)

---

### V1.0.49 — 2026-09 · `289056a`
**ธีม:** เพลงพื้นหลัง  
- ปลดล็อก BGM ให้เริ่มหลังแตะครั้งแรกได้ถูกต้อง

---

### V1.0.48 — 2026-09 · `cfa2ee8`
**ธีม:** ธีมอายุ  
- เก็บปีเกิดจากอายุที่กรอก → ธีมเปลี่ยนตามปีปฏิทิน

---

### V1.0.47 — 2026-09 · `ae77c66`
**ธีม:** ปฏิรูปห้องออนไลน์ครั้งใหญ่  

- ไม่มีบอทแทนคนเมื่อหมดเวลา (แถวว่าง)
- บอทแข่งในล็อบบี้ (เพิ่ม/เตะ)
- แชร์ลิงก์ห้อง (รุ่นแรกมีหลายปุ่ม)
- UI เหมาะอายุมาก / พร้อม-เตะ / โอนหัวห้อง ฯลฯ

---

### V1.0.40 — `3a0cc3a` … `0d8b0ca`
**ธีม:** ธีมอายุ · พร้อม/เตะ · คนออนไลน์ · ส่งเครื่อง  

- อายุ → ธีมสี
- ล็อบบี้ ready / kick
- แบดจ์ผู้เล่นออนไลน์
- นับถอยหลังส่งเครื่อง / ข้ามด้วยปุ่มถัดไปได้
- ขยายคอลัมน์ feedback โหมดยาก (5 ไอคอน)

---

### V1.0.30 — `f76f0ed` … `58f1f40`
**ธีม:** ระดับความยาก + เพลง  

- ง่าย / ปกติ / ยาก / ยากมาก
- เพลง lofi + สวิตช์เปิดปิด
- ปรับ layout setup / play / มือถือ

---

### V1.0.20 — `dcdd7b5` … `82108fb`
**ธีม:** เกมออนไลน์และกระดาษ  

- จบทันทีเมื่อทายถูก
- โชว์เฉลยบนหัวกระดาษหลังจบ
- ไอคอน feedback คลาสสิก / ดาว / จัดคอลัมน์
- ซ่อนกระดาษคนอื่น; ตัวจับเวลาต่อแถว (ยุคมีบอทแทน — ภายหลังยกเลิกใน V1.0.47)

---

### V1.0.10 — `3f297d5` … `8fa8cf9`
**ธีม:** สมาชิก / ล็อบบี้ / แขก  

- Firebase membership, ลีดเดอร์บอร์ดตามโหมด
- ชั่วคราวไม่บังคับล็อกอิน → เล่นเป็นแขก
- แก้ห้องออนไลน์โดยไม่ต้องมี Firebase Admin
- ช่องชื่อแสดงผลสำหรับแขก

---

### V1.0.00 — `e1485f1` … `dd38bd0`
**ธีม:** เกมตั้งต้น  

- เกมทายตัวเลข คนเดียว / ส่งเครื่อง / คนละเครื่อง
- เอกสาร deploy GitHub + Render
- UI สไตล์เด็ก / ปรับ tagline ครั้งแรก

---

## แผนที่ Commit → เวอร์ชัน (ย้อนละเอียด)

| เวอร์ชัน | Commit | ข้อความสั้น |
|---------|--------|-------------|
| V1.2.02 | *(รอบนี้)* | Fix last keypad digit not grayed |
| V1.2.01 | `1d10ddd` | Version badge in document flow |
| V1.2.00 | `239cd26` | Mode counts, age reset, stats by difficulty, UI copy |
| V1.1.00 | *(รอบก่อน)* | ประวัติ + แสดงเวอร์ชันบนเว็บ |
| V1.0.52 | `5d183ca` | Update setup tagline wording |
| V1.0.51 | `849a511` | Share / bot papers / leave confirm |
| V1.0.50 | `e98a81e` | Persist setup and room seat |
| V1.0.49 | `289056a` | Fix BGM unlock |
| V1.0.48 | `cfa2ee8` | Birth year age themes |
| V1.0.47 | `ae77c66` | Online rooms overhaul |
| V1.0.46 | `3a0cc3a` | Widen hard-mode feedback |
| V1.0.45 | `1a19e9e` | Skip handoff timer |
| V1.0.44 | `436ec79` | Presence / handoff / feedback fixes |
| V1.0.43 | `0d8b0ca` | Age themes, ready/kick, presence |
| V1.0.42 | `f76f0ed` | Music switch, mobile/room UX |
| V1.0.41 | `2da5fa0` | Restore .screen layout |
| V1.0.40 | `161abaa` | Setup/play UI polish |
| V1.0.39 | `a648d1f` | Lofi BGM |
| V1.0.38 | `58f1f40` | Difficulty modes |
| V1.0.37 | `dcdd7b5` | Single star legend |
| V1.0.36 | `0e06aa0` | Classic feedback icons |
| V1.0.35 | `1523d86` | Paper size / feedback column |
| V1.0.34 | `bfb4b66` | Paper feedback / bot timers |
| V1.0.33 | `4dcfa56` | Reveal answer digits |
| V1.0.32 | `82108fb` | End on correct guess |
| V1.0.31 | `3f297d5` | Guest display name |
| V1.0.30 | `e5fbb02` | Guest rooms without Admin |
| V1.0.29 | `f246b6b` | Skip forced login |
| V1.0.28 | `8fa8cf9` | Firebase / lobby / leaderboards |
| V1.0.10 | `2a97ba3`–`dd38bd0` | Tagline layout |
| V1.0.00 | `e1485f1` | Initial game |

*(เลขย่อยในตารางช่วงต้นใช้สำหรับ map commit; นับต่อเนื่องจาก V1.1.00 เป็นต้นไป)*

---

## หมายเหตุให้ AI ค่ายอื่น

1. อ่านไฟล์นี้ก่อนลงมือแก้เสมอ แล้วดู `version.js` ว่าตรงกับ “เวอร์ชันปัจจุบัน” หรือไม่  
2. อย่าคืนพฤติกรรม **บอทแทนคนเมื่อหมดเวลา** — ถูกยกเลิกตั้งแต่วุ่น V1.0.47  
3. แชร์ล็อบบี้ต้องเหลือแค่ **คัดลอกลิงก์ + แชร์**  
4. ข้อความ tagline ปัจจุบันอย่าเปลี่ยนกลับเองโดยไม่ถูกขอ  
5. หลังแก้: อัปเดตเวอร์ชัน + ไฟล์นี้ + commit/push ตามที่เจ้าของโปรเจกต์ขอ  
6. ภาษา UI เป็นภาษาไทย; โค้ด/commit message ใช้ภาษาอังกฤษได้ตามสไตล์ repo
